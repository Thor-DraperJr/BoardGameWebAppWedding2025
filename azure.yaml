# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: simple-game-state-test
metadata:
  template: simple-game-state-test@1.0.0

services:
  web:
    project: .
    language: js
    host: staticwebapp
